---
title: Retriever
---

A retriever in vectorstores is what is used to fetch `Node`s from an index using a query string.

- [VectorIndexRetriever](/api/classes/vectorindexretriever/) will fetch the top-k most similar nodes. Ideal for dense retrieval to find most relevant nodes.

```typescript
const retriever = vectorIndex.asRetriever({
  similarityTopK: 3,
});

// Fetch nodes!
const nodesWithScore = await retriever.retrieve({ query: "query string" });
```
